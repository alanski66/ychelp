<!-- Bootstrap core JavaScript -->
<script src="{{ baseUrl }}/js/dist-min.js"></script>
{% if tracking.includeTrackingScripts | length %}
   {{ tracking.includeTrackingScripts | raw }}
{% endif %}
<script>
   var slidecount = "";
   // $('.slider').slick({
   //
   // });


   $(document).ready(function () {

      $('.main-carousel').flickity({
         // options
         // cellAlign: 'left',
         initialIndex: '.is-initial-select',
         // wrapAround: true,
         pageDots: false,
         contain: true
      });

      $('.supporter-carousel').flickity({
         // options
         // cellAlign: 'left',
         initialIndex: '.is-initial-select',
         // wrapAround: true,
         pageDots: false,

         // contain: true
      });
      
   });
</script>